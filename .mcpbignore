# Build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Dev files
.git/
.github/
.venv/
.env
.env.*
*.log

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude config (not part of the extension)
.claude/

# Kangentic (local AI workflow tool — sessions can contain conversation history)
.kangentic/
kangentic.json

# Plan files
plans/

# Dev-only tooling (icon extraction) — not part of the extension
tools/

# Dev-only: test suites and their pytest cache. They cannot run from the
# installed bundle anyway — the permissioned tier needs Claude Desktop to be
# the responsible process, not a shell in this directory.
tests/
.pytest_cache/
